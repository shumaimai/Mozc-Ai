# Copyright 2024 AI Mozc IME Project
# Bazel MODULE file (bzlmod) - Required for Bazel 8+
#
# This file replaces WORKSPACE for dependency management.
# See: https://bazel.build/external/migration

module(
    name = "ai_mozc",
    version = "1.0.1",
)

# ============================================================================
# Core Dependencies
# Note: Versions are aligned with Bazel Central Registry (BCR)
# ============================================================================

# Rules for C++ (required for cc_library, cc_binary, cc_test)
bazel_dep(name = "rules_cc", version = "0.1.1")

# Platform definitions
bazel_dep(name = "platforms", version = "0.0.11")

# Google Test for unit testing
bazel_dep(name = "googletest", version = "1.14.0.bcr.1")

# ============================================================================
# Optional Dependencies (for future use)
# ============================================================================

# Abseil C++ library
# bazel_dep(name = "abseil-cpp", version = "20240116.2")

# Protocol Buffers (uncomment when needed)
# bazel_dep(name = "protobuf", version = "21.7")
# bazel_dep(name = "rules_proto", version = "5.3.0-21.7")
